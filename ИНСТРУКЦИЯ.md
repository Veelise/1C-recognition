# Инструкция по работе с программой «1С — ИИ распознавание чертежей»

## Содержание

1. [Запуск программы](#запуск-программы)
2. [Подключение к БД](#подключение-к-бд)
3. [Workflow программы](#workflow-программы)
4. [Интерфейс программы](#интерфейс-программы)
5. [Таблицы и структура БД](#таблицы-и-структура-бд)
6. [Возможные ошибки](#возможные-ошибки)
7. [Установка зависимостей](#установка-зависимостей)

---

## Запуск программы

```bash
cd 1C-recognition
python db_app_v2.py
```

---

## Подключение к БД

При запуске появится окно подключения. Введите параметры:

| Параметр | Значение |
|----------|----------|
| Host | localhost (или адрес сервера) |
| Port | 5432 |
| Database | **shuvi_test** |
| User | ваш пользователь PostgreSQL |
| Password | ваш пароль |

---

## Workflow программы

Программа имеет 4 этапа работы с чертежами.

### Этап 1: 📥 Загрузка

**Цель:** Загрузить PDF-чертеж в систему

**Порядок действий:**

1. **Выберите сотрудника** — кнопка "Сотрудник" в правом верхнем углу
2. Нажмите **"Загрузить PDF"**
3. Выберите PDF-файл чертежа
4. Введите номер/название чертежа
5. Чертеж сохраняется в БД → автоматический переход к этапу 2

---

### Этап 2: 🔧 Предобработка

**Цель:** Оценить качество и подготовить изображение

**Порядок действий:**

1. Выберите чертеж из списка
2. Нажмите **"Обработать"** — система:
   - Оценит уровень искажений
   - Выполнит бинаризацию, выравнивание, удаление шума
3. Или нажмите **"Пропустить"** — перейти к OCR без обработки

**Логика контроля качества:**

- **Уровень искажений ≤ 15%** → автоматическая предобработка
- **Уровень искажений > 15%**:
  - Система предложит улучшить читаемость
  - Если "Да" → предобработка
  - Если "Нет" → спросит "Принудительно запустить?"
    - "Да" → предобработка
    - "Нет" → спросит "Пропустить предобработку?"
      - "Да" → переход к OCR без обработки
      - "Нет" → остановка

**Выбор области:**

- **Да** → обрабатывать весь файл
- **Нет** → выбрать область (в данной версии = весь файл)

---

### Этап 3: 🔍 OCR

**Цель:** Распознать текст с чертежа

**Порядок действий:**

1. Выберите чертеж из списка
2. Нажмите:
   - **"Запустить OCR"** — автоматическое распознавание (EasyOCR)
   - **"Вручную"** — заполнить данные вручную
3. Дождитесь распознавания (10-60 секунд)
4. Результат сохраняется в PRO (таблица БД)
5. Автоматический переход к этапу 4

---

### Этап 4: ✅ Проверка

**Цель:** Проверить и валидировать данные

**Порядок действий:**

1. Просмотрите распознанные данные
2. При необходимости нажмите **"Редактировать"** — исправьте данные
3. Нажмите **"Валидировать → FRO"** — создаётся:
   - Запись в таблице FRO (финальный результат)
   - Запись в таблице ARCH_OF_DRAWS (архив)

---

## Интерфейс программы

```
┌─────────────────────────────────────────────────────────────────────┐
│  [1. Загрузка] [2. Предобработка] [3. OCR] [4. Проверка]  [Сотрудник]│
├──────────────────────┬──────────────────────────────────────────────┤
│                      │                                              │
│   СПИСОК ЧЕРТЕЖЕЙ    │           ДЕТАЛИ И ДЕЙСТВИЯ                  │
│                      │                                              │
│   (дерево/таблица)   │   - Инструкции                               │
│                      │   - Результаты OCR                           │
│                      │   - Кнопки действий                          │
│                      │                                              │
├──────────────────────┴──────────────────────────────────────────────┤
│  [🔄 Обновить]                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Навигация по этапам

| Этап | Кнопка | Что показывает |
|------|--------|----------------|
| 1 | Загрузка | PRIMARY_DRAWINGS |
| 2 | Предобработка | PRIMARY_DRAWINGS без PRO |
| 3 | OCR | PRO (результаты OCR) |
| 4 | Проверка | PRO для валидации |

---

## Таблицы и структура БД

### Схема связей между таблицами

```
EMPLOYEES
    │
    ├──→ SKETCH_DRAWINGS (idEmployee)
    ├──→ PRIMARY_DRAWINGS (idEmployee)
    ├──→ ARCH_OF_DRAWS (id_employeePrmArch)
    │
SKETCH_DRAWINGS ──────→ PRIMARY_DRAWINGS (AssociatedWith → idSkDrav)
    │
PRIMARY_DRAWINGS ─────→ PRO (idPrimaryDrawing)
    │
PRO ─────────────────→ FRO (pro_id)
    │
FRO ─────────────────→ ARCH_OF_DRAWS (NumDravFROArch)
```

---

### 1. EMPLOYEES — Сотрудники

Хранит информацию о пользователях системы.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idEmployee` | BIGSERIAL | **PK** | ID сотрудника (автоинкремент) |
| `Post` | VARCHAR(100) | | Должность |
| `FullName` | VARCHAR(255) | **NOT NULL** | ФИО сотрудника |
| `Role` | VARCHAR(50) | DEFAULT 'operator' | Роль: admin, operator, validator |

**Ограничения:**
- `Role` CHECK (Role IN ('admin', 'operator', 'validator'))

**Индексы:**
- `idx_employees_role` — по полю Role

---

### 2. SKETCH_DRAWINGS — Эскизы чертежей

Исходные файлы чертежей (скан/фото PDF).

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idSkDrav` | BIGSERIAL | **PK** | ID эскиза |
| `SFilePath` | VARCHAR(500) | | Путь к файлу |
| `DateAdded` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата добавления |
| `NumName` | VARCHAR(255) | | Номер/название чертежа |
| `idEmployee` | BIGINT | **FK** → EMPLOYEES(idEmployee) | Кто загрузил |

**Ограничения:**
- `idEmployee` ON DELETE SET NULL

**Индексы:**
- `idx_sketch_date` — по DateAdded DESC
- `idx_sketch_employee` — по idEmployee

---

### 3. PRIMARY_DRAWINGS — Первичные чертежи

Обработанные чертежи после загрузки.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `id` | BIGSERIAL | **PK** | ID чертежа |
| `FilePath` | VARCHAR(500) | | Путь к файлу |
| `AssociatedWith` | VARCHAR(100) | | Связь с эскизом (idSkDrav) |
| `NeedToImprove` | BOOLEAN | DEFAULT FALSE | Требует улучшения |
| `idEmployee` | BIGINT | **FK** → EMPLOYEES(idEmployee) | Кто загрузил |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |

**Ограничения:**
- `idEmployee` ON DELETE SET NULL

**Индексы:**
- `idx_primary_employee` — по idEmployee
- `idx_primary_need_improve` — по NeedToImprove

---

### 4. PRO — Результаты OCR

Данные, извлечённые OCR из чертежа.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idPrmRes` | BIGSERIAL | **PK** | ID результата |
| `NameDrav` | VARCHAR(255) | | Наименование чертежа |
| `Designation` | VARCHAR(100) | | Обозначение |
| `ProjectCode` | BIGINT | | Код проекта |
| `Dev` | VARCHAR(255) | | Разработчик |
| `DateOriginalCreation` | DATE | | Дата создания оригинала |
| `OriginalPaperFormat` | VARCHAR(20) | | Формат (А1, А4...) |
| `NumberOfSheets` | BIGINT | DEFAULT 1 | Количество листов |
| `Notes` | TEXT | | Примечания (распознанный текст) |
| `NumDrav` | BIGINT | | Номер чертежа |
| `idPrimaryDrawing` | BIGINT | **FK** → PRIMARY_DRAWINGS(id) | Связь с первичным чертежом |
| `validated` | BOOLEAN | DEFAULT FALSE | Валидирован |
| `validated_by` | BIGINT | **FK** → EMPLOYEES(idEmployee) | Кто валидировал |
| `validation_date` | TIMESTAMP | | Дата валидации |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания записи |

**Ограничения:**
- `idPrimaryDrawing` ON DELETE SET NULL
- `validated_by` ON DELETE SET NULL

**Индексы:**
- `idx_pro_drawing` — по idPrimaryDrawing
- `idx_pro_validated` — по validated
- `idx_pro_numdrav` — по NumDrav

---

### 5. FRO — Финальные результаты

Проверенные и валидированные данные.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idFnlRes` | BIGSERIAL | **PK** | ID результата |
| `NameDrav` | VARCHAR(255) | | Наименование |
| `Designation` | VARCHAR(100) | | Обозначение |
| `ProjectCode` | BIGINT | | Код проекта |
| `Dev` | VARCHAR(255) | | Разработчик |
| `DateOriginalCreation` | DATE | | Дата создания |
| `OriginalPaperFormat` | VARCHAR(20) | | Формат |
| `NumberOfSheets` | BIGINT | DEFAULT 1 | Количество листов |
| `NumDrav` | BIGINT | | Номер чертежа |
| `pro_id` | BIGINT | **FK** → PRO(idPrmRes) | Связь с PRO |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |

**Ограничения:**
- `pro_id` ON DELETE SET NULL

**Индексы:**
- `idx_fro_pro` — по pro_id
- `idx_fro_numdrav` — по NumDrav

---

### 6. ARCH_OF_DRAWS — Архив чертежей

Долгосрочное хранение финальных чертежей.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idPrmArch` | BIGSERIAL | **PK** | ID архива |
| `FilePathPrmArch` | VARCHAR(500) | | Путь к файлу |
| `AssociatedWithPrmArch` | VARCHAR(100) | | Связь |
| `NumDravFROArch` | BIGINT | | Номер чертежа |
| `NameDravFROArch` | VARCHAR(255) | | Наименование |
| `DesignationFROArch` | VARCHAR(100) | | Обозначение |
| `ProjectCodeFROArch` | BIGINT | | Код проекта |
| `DevFROArch` | VARCHAR(255) | | Разработчик |
| `DateOriginalCreationFROArch` | DATE | | Дата создания |
| `OriginalPaperFormatFROArch` | VARCHAR(20) | | Формат |
| `NumberOfSheetsFROArch` | BIGINT | DEFAULT 1 | Количество листов |
| `SaveDateArch` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата архивации |
| `MonthCheckArch` | BOOLEAN | DEFAULT FALSE | Месячная проверка |
| `SFilePathArch` | VARCHAR(500) | | Путь к эскизу |
| `DateAddedArch` | TIMESTAMP | | Дата добавления |
| `NumNameArch` | VARCHAR(255) | | Название |
| `UploadedEdmployeeIDArch` | VARCHAR(100) | | ID загрузившего |
| `id_employeePrmArch` | BIGINT | **FK** → EMPLOYEES(idEmployee) | Сотрудник |

**Ограничения:**
- `id_employeePrmArch` ON DELETE SET NULL

**Индексы:**
- `idx_archive_employee` — по id_employeePrmArch
- `idx_archive_numdrav` — по NumDravFROArch
- `idx_archive_date` — по SaveDateArch DESC

---

### 7. DRAWING_TAMPLATES — Шаблоны (ГОСТ)

Эталоны для сравнения чертежей.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idGOST` | BIGSERIAL | **PK** | ID шаблона |
| `GOSTName` | VARCHAR(255) | | Название ГОСТа |
| `DateGOST` | DATE | | Дата ГОСТа |
| `Relevance` | BOOLEAN | DEFAULT TRUE | Актуальность |
| `TemplatesPhotoPath` | VARCHAR(500) | | Путь к фото |
| `DravingTemplatesPath` | VARCHAR(500) | | Путь к шаблону |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |

**Индексы:**
- `idx_templates_relevance` — по Relevance

---

### 8. TEMPLATE_AREA — Области шаблона

Зоны на шаблоне для поиска данных.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `idArea` | BIGSERIAL | **PK** | ID области |
| `CoordsX1` | BIGINT | | X1 координата |
| `CoordsY1` | BIGINT | | Y1 координата |
| `CoordsX2` | BIGINT | | X2 координата |
| `CoordsY2` | BIGINT | | Y2 координата |
| `idGOST` | BIGINT | **FK** → DRAWING_TAMPLATES(idGOST) | ID шаблона |
| `AreaName` | VARCHAR(100) | | Название области |

**Ограничения:**
- `idGOST` ON DELETE CASCADE

**Индексы:**
- `idx_template_area_gost` — по idGOST

---

### 9. DRAWING_VERSIONS — Версии чертежей

История исправлений чертежей.

| Поле | Тип | Ключ | Описание |
|------|-----|------|----------|
| `id_version` | BIGSERIAL | **PK** | ID версии |
| `original_drawing_id` | BIGINT | **FK** → PRIMARY_DRAWINGS(id) | Оригинал |
| `corrected_drawing_id` | BIGINT | **FK** → PRIMARY_DRAWINGS(id) | Исправленный |
| `correction_date` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата исправления |
| `idEmployee` | BIGINT | **FK** → EMPLOYEES(idEmployee) | Кто исправил |
| `Notes` | TEXT | | Примечания |

**Ограничения:**
- `original_drawing_id` ON DELETE CASCADE
- `corrected_drawing_id` ON DELETE CASCADE
- `idEmployee` ON DELETE SET NULL

**Индексы:**
- `idx_versions_original` — по original_drawing_id
- `idx_versions_corrected` — по corrected_drawing_id

---

## Поток данных (workflow)

```
Загрузка PDF (Этап 1)
    ↓
SKETCH_DRAWINGS (эскиз)
    ↓
PRIMARY_DRAWINGS (первичный чертеж)
    ↓
Предобработка (Этап 2)
    → Оценка искажений
    → Бинаризация, выравнивание, удаление шума
    ↓
OCR (Этап 3)
    → EasyOCR → извлечение текста
    ↓
PRO (результат распознавания)
    ↓
Проверка (Этап 4)
    → Редактирование данных
    → Валидация
    ↓
FRO (финальный результат) + ARCH_OF_DRAWS (архив)
```

---

## Возможные ошибки

| Ошибка | Решение |
|--------|---------|
| "Нет подключения к БД" | Проверьте параметры подключения |
| "Выберите сотрудника" | Нажмите "Сотрудник" и выберите сотрудника |
| "Модуль easyocr не найден" | Установите: `pip install easyocr` |
| "Модуль fitz не найден" | Установите: `pip install PyMuPDF` |
| "Выберите чертеж" | Выберите строку в списке слева |
| "Файл не найден" | Проверьте путь к PDF-файлу |

---

## Установка зависимостей

```bash
pip install PyMuPDF Pillow opencv-python numpy easyocr psycopg2-binary
```

**Дополнительно для предобработки:**

```bash
pip install opencv-python
```

---

## Сочетания клавиш

| Действие | Кнопка |
|----------|--------|
| Обновить данные | Кнопка "Обновить" |
| Выбор сотрудника | Кнопка "Сотрудник" |
| Закрыть программу | Alt+F4 |

---

## Тестовые данные

При создании БД автоматически добавляются сотрудники:

| ID | ФИО | Должность | Роль |
|----|-----|-----------|------|
| 1 | Иванов Иван Иванович | Оператор | operator |
| 2 | Петров Петр Петрович | Валидатор | validator |
| 3 | Сидоров Сидор Сидорович | Администратор | admin |
